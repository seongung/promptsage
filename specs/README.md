# Specs Overview

This repository tracks Prompt Sage features scoped under `specs/`.

| Feature | Branch | Description |
|---------|--------|-------------|
| [`001-prompt-sage-tui`](001-prompt-sage-tui/) | `001-prompt-sage-tui` | Core Prompt Sage TUI experience for browsing prompts, running reviews, filtering, and exporting results. |
| [`001-auto-bulk-review`](001-auto-bulk-review/) | `001-auto-bulk-review` | Adds automatic bulk reviews on launch, a progress overlay, and cancellation controls for startup batches. |

Use `/speckit.tasks` within each feature directory to regenerate task plans after spec updates.
